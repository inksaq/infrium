package com.infrium.game.handlers;


import com.infrium.core.api.handlers.Handler;
import com.infrium.game.Settlements;

public class BuildingHandler extends Handler<Settlements> {

/*    public HashSet<Settlement> loadedSettlements;

    @Override

    public void enable(Settlements plugin) {
        if (loadedSettlements == null) loadedSettlements = new HashSet<>();



    }

    @Override
    public void disable(Settlements plugin) {
        super.disable(plugin);
    }*/
}
